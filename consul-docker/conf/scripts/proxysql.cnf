# Consul User configuration
# Here the user can configure values to avoid passing them via command line.
# This file is read by the following scripts:
# * start-proxysql.sh
# * register-services.sh
# * cleanup.sh

MYSQL_SERVICE_NAME='mysql'
MYSQL_PORT_MYSQL='3306'
MYSQL_USER_MYSQL='root'
MYSQL_PASSWORD_MYSQL='root'

PROXYSQL_SERVICE_NAME='proxysql'
PROXYSQL_PORT_ADMIN='6032'
PROXYSQL_USER_ADMIN='admin'
PROXYSQL_PASSWORD_ADMIN='admin'

