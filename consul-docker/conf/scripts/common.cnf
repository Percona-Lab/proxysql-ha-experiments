# Consul User configuration
# Here the user can configure values to avoid passing them via command line.
# This file is read before other files. So values written here can be overridden.

# by default, home dir
if [ -z $CONFIG_DIR ];
then
		CONFIG_DIR='./conf'
fi

